function r = calcResidual(Cam_z_i,Cam_zHat_i,validIndex)
validSampleNum = (validIndex,2);
r = [];

for i = 1:validSampleNum
    r_i =   Cam_z_i(:,validIndex)- Cam_zHat_i(:,validIndex);
    
end
r = Cam_z_i(:,validIndex)- Cam_zHat_i(:,validIndex);
r = reshape(r,[size(validIndex,2)*2,1]);



end